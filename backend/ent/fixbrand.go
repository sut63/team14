// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/tanapon395/playlist-video/ent/fixbrand"
)

// Fixbrand is the model entity for the Fixbrand schema.
type Fixbrand struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Fixbrandname holds the value of the "fixbrandname" field.
	Fixbrandname string `json:"fixbrandname,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FixbrandQuery when eager-loading is set.
	Edges FixbrandEdges `json:"edges"`
}

// FixbrandEdges holds the relations/edges for other nodes in the graph.
type FixbrandEdges struct {
	// Fix holds the value of the fix edge.
	Fix []*Fix
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// FixOrErr returns the Fix value or an error if the edge
// was not loaded in eager-loading.
func (e FixbrandEdges) FixOrErr() ([]*Fix, error) {
	if e.loadedTypes[0] {
		return e.Fix, nil
	}
	return nil, &NotLoadedError{edge: "fix"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Fixbrand) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // fixbrandname
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Fixbrand fields.
func (f *Fixbrand) assignValues(values ...interface{}) error {
	if m, n := len(values), len(fixbrand.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	f.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field fixbrandname", values[0])
	} else if value.Valid {
		f.Fixbrandname = value.String
	}
	return nil
}

// QueryFix queries the fix edge of the Fixbrand.
func (f *Fixbrand) QueryFix() *FixQuery {
	return (&FixbrandClient{config: f.config}).QueryFix(f)
}

// Update returns a builder for updating this Fixbrand.
// Note that, you need to call Fixbrand.Unwrap() before calling this method, if this Fixbrand
// was returned from a transaction, and the transaction was committed or rolled back.
func (f *Fixbrand) Update() *FixbrandUpdateOne {
	return (&FixbrandClient{config: f.config}).UpdateOne(f)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (f *Fixbrand) Unwrap() *Fixbrand {
	tx, ok := f.config.driver.(*txDriver)
	if !ok {
		panic("ent: Fixbrand is not a transactional entity")
	}
	f.config.driver = tx.drv
	return f
}

// String implements the fmt.Stringer.
func (f *Fixbrand) String() string {
	var builder strings.Builder
	builder.WriteString("Fixbrand(")
	builder.WriteString(fmt.Sprintf("id=%v", f.ID))
	builder.WriteString(", fixbrandname=")
	builder.WriteString(f.Fixbrandname)
	builder.WriteByte(')')
	return builder.String()
}

// Fixbrands is a parsable slice of Fixbrand.
type Fixbrands []*Fixbrand

func (f Fixbrands) config(cfg config) {
	for _i := range f {
		f[_i].config = cfg
	}
}
